<%= form_for([@team, @team.players.build]) do |f| %>


  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit  class: "btn btn-success" %>
  </div>
<% end %>
